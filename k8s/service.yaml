apiVersion: v1
kind: Service
metadata:
  name: devops-journey-service
  namespace: web-app-devops
  labels:
    release: my-monitor
spec:
  type: ClusterIP
  selector:
    app: web-site
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
